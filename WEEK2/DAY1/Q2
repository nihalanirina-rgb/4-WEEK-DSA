class Solution {
public:
    int firstUniqChar(string s) {
        for(int i = 0; i < s.size(); i++) {
            char k = s[i];
            int c = 0;

            for(int j = 0; j < s.size(); j++) {
                if(k == s[j]) {
                    c++;
                }
            }

            if(c == 1) {
                return i;
            }
        }

        return -1;
    }
};
