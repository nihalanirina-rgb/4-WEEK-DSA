class Solution {
public:
    int maxProfit(vector<int>& prices) {
        int mn=prices[0];
        int mxpro=0;
        for(int i=0;i<prices.size();i++){
            mn=min(mn,prices[i]);
            int mx=prices[i]-mn;
            mxpro=max(mxpro,mx);
        }return mxpro;
    }
};
